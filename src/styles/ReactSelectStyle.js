export const customStyle = {
	control: (baseStyle) => ({
		...baseStyle,
		borderRadius: '1.75em',
		padding: '0.5em',
	}),
	menu: (baseStyle) => ({
		...baseStyle,
		borderRadius: '1.75em',
	}),
	menuList: (baseStyle) => ({
		...baseStyle,
		borderRadius: '1.75em',
		padding: '.5em 1em'
	}),
	option: (baseStyle) => ({
		...baseStyle,
		borderRadius: '.5em',
		padding: '.75em',
		margin: '.5em 0'
	}), 
}
